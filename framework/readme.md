This folder contains WW custom framework files.

It should not have any non-generic code (ie it should be possible 
to just copy this into another project without changing anything)

namespace Framework; 
